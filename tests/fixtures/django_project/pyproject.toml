[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "gway-django-fixture"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = ["Django>=4.2"]

[tool.setuptools.packages.find]
where = ["."]
include = ["fixture_project*"]
